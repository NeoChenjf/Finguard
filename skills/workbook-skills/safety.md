# 安全与终端命令规范（Safety）

终端命令风险规则
- 禁止自动执行高风险命令（远程下载、格式化、系统级改动等）。
- 重要或高权限命令必须先说明影响并等待用户确认，执行后汇报结果并记录日志。

敏感信息处理
- 不得泄露、存储或传播 API Key、密码、私钥等敏感信息。
- 日志中出现敏感字段需脱敏或省略具体值。

自动化限制
- 自动化步骤应尽量使用本地、可复现的命令（避免全局变更）。
