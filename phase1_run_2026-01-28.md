# Phase 1 自动化运行验收记录（2026-01-28）

执行时间：2026-01-28

执行摘要：自动构建并启动 `finguard` 可执行，轮询 `/health` 直至就绪，调用 `/api/v1/plan`，收集响应并保存到临时目录。服务以后台进程形式启动并在完成后停止。

主要命令（摘要）：

```powershell
# 构建（先前已完成）
cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=d:/AI_Investment/tools/vcpkg/scripts/buildsystems/vcpkg.cmake
cmake --build build

# 后台启动并重定向日志
Start-Process -FilePath 'd:\AI_Investment\finguard\build\Debug\finguard.exe' -RedirectStandardOutput 'd:\AI_Investment\tools\temp\finguard_out.log' -RedirectStandardError 'd:\AI_Investment\tools\temp\finguard_err.log' -PassThru

# 轮询并调用接口（示例）
Invoke-RestMethod -Uri 'http://127.0.0.1:8080/health' -Method Get
Invoke-RestMethod -Uri 'http://127.0.0.1:8080/api/v1/plan' -Method Post -ContentType 'application/json' -Body '{"profile":{"age":35,"risk":"medium","capital":500000}}'

# 停止后台服务（示例，使用实际 PID）
Stop-Process -Id <pid> -Force
```

结果（采集）：

- `/health` 响应：

```json
{"status":"ok"}
```

- `/api/v1/plan` 响应（已压缩）：

```json
{"proposed_portfolio":{"BND":0.4,"CASH":0.2,"GLD":0.1,"SPY":0.3},"rationale":"当前为 mock 输出，后续将接入画像解析与规则引擎。","rebalancing_actions":[],"risk_report":{"status":"PASS","triggered_rules":[]}}
```

- 服务日志（重定向文件）：
  - `d:\AI_Investment\tools\temp\finguard_out.log`：内容为空或无额外信息。
  - `d:\AI_Investment\tools\temp\finguard_err.log`：文件不存在或为空。

附注：自动化执行过程中服务可在短时间内启动并返回预期的 mock 响应；未发现运行时错误输出。建议将这些步骤纳入 CI 流程并将日志与响应作为验收证据保存到 `phase1.md` 的验收记录中。
